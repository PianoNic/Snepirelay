# Snepirelay

The jam relay for Snepilatch. It lets any account host a jam, free ones included, by syncing the host and guests through this server instead of spfy. No audio passes through it: every member plays on their own account, and the relay only carries playback state, commands and membership.
